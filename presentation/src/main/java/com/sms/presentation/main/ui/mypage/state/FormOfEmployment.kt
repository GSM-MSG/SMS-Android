package com.sms.presentation.main.ui.mypage.state

enum class FormOfEmployment(val text: String) {
    FULL_TIME("정규직"),
    TEMPORARY("비정규직"),
    CONTRACT("계약직"),
    INTERN("인턴"),
    NOT_SELECT("")
}
