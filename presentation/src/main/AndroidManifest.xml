<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.sms.presentation.main.ui.login"/>
        <package android:name="com.sms.presentation.main.ui.detail"/>
    </queries>


    <application>

        <!-- setting fcm default icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_sms_white" />

        <service
            android:name=".service.SMSFirebaseMessagingService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".main.ui.fill_out_information.FillOutInformationActivity"
            android:exported="false"
            android:label="@string/title_activity_fill_out_information"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".main.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.ui.main.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            tools:ignore="DiscouragedApi" />
        <activity
            android:name=".main.ui.teacher_registration.TeacherRegistrationActivity"
            android:exported="false"
            android:label="TeacherRegistrationActivity"/>
    </application>

</manifest>